import { StyleSheet, Text, View } from "react-native";

const WishlistScreen = () => (
    <View style={styles.screen}>
      <Text>Browse different products</Text>
    </View>
  );

const styles = StyleSheet.create({
  screen: {
    padding: 10,
  },
});

export default WishlistScreen;